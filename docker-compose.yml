version: '3.9'

services:


  noteit:
    container_name: noteit
    build:
      context: .
      dockerfile: Dockerfile
 
    environment:
      - SECRET_KEY1="qwertyuiopasdfghjklzxcvbnmgenratedfjsdfjs"

    volumes:
      - .:/usr/src/app

    ports:  
      - "8000:80"